package com.groupeseb.mediaimport.model;

import java.io.InputStream;

public class ProgramDTO extends LKVFileDTO {

	public ProgramDTO(String key, String extension, InputStream inputStream) {
		super(key, extension, inputStream);
	}
}
