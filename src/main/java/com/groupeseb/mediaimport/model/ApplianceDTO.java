package com.groupeseb.mediaimport.model;

import java.io.InputStream;

public class ApplianceDTO extends LKVFileDTO {

	public ApplianceDTO(String key, String extension, InputStream inputStream) {
		super(key, extension, inputStream);
	}
}
